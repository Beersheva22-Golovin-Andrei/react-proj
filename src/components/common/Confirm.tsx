import {Box, Dialog} from '@mui/material';

type Props = {

}

const Confirm: React.FC<Props> = ({}) => {
    return <Box>
        <Dialog open={false}/>
    </Box>
}

export default Confirm;